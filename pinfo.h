#ifndef __pinfo_H
#define __pinfo_H

#include "headers.h"

// char *getShellDir(char *curr);
// char *readFileLine(char *path, int line_num);
// int isPrefix(int home_len, int cwd_len, char *curr);
// char *getRootDir(char *inppath);
void pinfocmd(char *list[250], int len);

#endif